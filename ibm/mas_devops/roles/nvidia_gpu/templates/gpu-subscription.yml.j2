---
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: gpu-operator-certified
  namespace: "{{ gpu_namespace }}"
spec:
  channel: "{{ gpu_channel }}"
  installPlanApproval: Automatic
  name: gpu-operator-certified
  source: "{{ gpu_source }}"
  sourceNamespace: "{{ gpu_source_namespace }}"
