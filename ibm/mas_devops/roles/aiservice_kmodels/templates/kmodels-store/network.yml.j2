---
apiVersion: v1
kind: Service
metadata:
  name: km-store
  namespace: mas-{{ aiservice_instance_id }}-aibroker
spec:
  ports:
    - name: https
      port: 8443
      targetPort: https
  selector:
    app: km-store
  type: ClusterIP
