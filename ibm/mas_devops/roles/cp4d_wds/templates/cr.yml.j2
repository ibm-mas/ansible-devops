apiVersion: discovery.watson.ibm.com/v1
kind: WatsonDiscovery
metadata:
  annotations:
    oppy.ibm.com/disable-rollback: 'true'
  name: wd
  namespace: {{ cpdnamespace }}
spec:
  license:
    accept: true
  version: "{{ wdsversion }}"
  shared:
    storageClassName: "{{ cpdstorageclass) }}"
  watsonGateway:
    version: main
