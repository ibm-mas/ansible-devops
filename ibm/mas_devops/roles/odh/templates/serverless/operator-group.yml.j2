---
apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: serverless-operator-operator-group
  namespace: "{{ serverless_namespace }}"
spec:
  targetNamespaces:
    - "{{ serverless_namespace }}"
