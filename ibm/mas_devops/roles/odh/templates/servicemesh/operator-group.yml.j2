---
apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: servicemesh-operator-operator-group
  namespace: "{{ service_mesh_namespace }}"
spec:
  targetNamespaces:
    - "{{ service_mesh_namespace }}"
