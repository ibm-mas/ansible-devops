# ArcGISWorkspace configuration
---
apiVersion: apps.mas.ibm.com/v1
kind: ArcGISWorkspace
metadata:
    name: "{{ mas_instance_id }}"
    namespace: "{{ arcgis_namespace }}"
    labels:
        mas.ibm.com/applicationId: arcgis
        mas.ibm.com/instanceId: "{{ mas_instance_id }}"
spec: {}
