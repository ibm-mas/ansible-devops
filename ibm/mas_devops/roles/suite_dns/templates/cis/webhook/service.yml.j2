apiVersion: v1
kind: Service
metadata:
  name: "cert-manager-webhook-ibm-cis"
  labels:
    app: "cert-manager-webhook-ibm-cis"
spec:
  type: "{{ cis_wekhook.service.type }}"
  ports:
    - port: {{ cis_wekhook.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    app: "cert-manager-webhook-ibm-cis"
