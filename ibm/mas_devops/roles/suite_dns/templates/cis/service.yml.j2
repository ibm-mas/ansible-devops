apiVersion: v1
kind: Service
metadata:
  name: "cert-manager-webhook-ibm-cis"
  labels:
    app: "cert-manager-webhook-ibm-cis"
spec:
  type: "{{ service.type }}"
  ports:
    - port: {{ service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    app: "cert-manager-webhook-ibm-cis"
