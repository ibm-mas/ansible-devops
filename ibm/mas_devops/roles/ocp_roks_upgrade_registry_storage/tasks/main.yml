---

# 1. Increase cluster image registry from default 100Gi to 400Gi (cp4d images consume lot of space)
# -------------------------------------------------------------------------------------
- name: "Increase cluster image registry storage"
  command: "bash {{ role_path }}/files/configImgRegStorage.sh {{ ibmcloud_apikey }} {{ image_registry_size }}"
