---
# Default application spec for Monitor
mas_appws_spec:
  bindings:
    iot: workspace
    jdbc: "{{ mas_appws_bindings_jdbc | default( 'system' , true) }}"
  settings:
    storage:
      storageClass: "{{ defaultStorageClasses.rwx }}"
      storageClassSize: "20Gi"
