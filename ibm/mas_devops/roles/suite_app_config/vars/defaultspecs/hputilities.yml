---
# Default application spec for HPUtilities
mas_app_ws_spec:
  bindings:
    watsonstudio: system
  components: '{{ mas_appws_components | default({}, true) }}'
  settings:
    watsonstudio:
      projectid: "{{ mas_app_cfg_wsl_product_id }}"
