---
- name: Load configuration variables
  vars:
    config_var_dir: "{{ mas_config_dir }}/{{ mas_app_id }}"
  include_tasks: "tasks/load-config-vars.yml"

- name: "Debug {{ mas_app_id }} information"
  debug:
    msg:
      - "WSL Product ID ............ {{ mas_app_cfg_wsl_product_id }}"
