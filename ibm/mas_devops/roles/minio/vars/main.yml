---
# minio operator
openshift_namespace: openshift-operators
minio_channel: "{{ lookup('env', 'MINIO_CHANNEL') | default('v1.0', true) }}"
minio_catalog_source: "{{ lookup('env', 'MINIO_CATALOG_SOURCE') | default('ibm-operator-catalog', true) }}"
