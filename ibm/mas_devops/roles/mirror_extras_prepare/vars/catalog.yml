---
extra_images:
  - name: cpopen/ibm-maximo-operator-catalog
    registry: "{{ dev_mode_airgap | ternary( artifactory_image_repo, icr.io) }}"
    tag: "{{ catalog_tag }}"
    digest: "{{ catalog_digest }}"
