---
extra_images:
  - name: cpopen/ibm-maximo-operator-catalog
    registry: "{{ dev_mode | ternary( artifactory_image_repo, icr.io) }}"
    tag: "{{ catalog_tag }}"
    digest: "{{ catalog_digest }}"
