{%- for image in extra_images %}
{{ image.registry }}/{{ image.name }}@{{ image.digest }}={{ registry_public_url_with_path }}/{{ image.name }}{{ ':' ~ image.tag if image.tag is defined else '' }}
{% endfor %}
