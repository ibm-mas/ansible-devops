galaxy_info:
  author: André Marcelino (@andrercm)
  description: Creates AWS policy (this role is used internally by other roles, it's not designed to be used as standalone role)
  company: IBM

  license: EPL-2.0

  min_ansible_version: 2.10

  platforms:
    - name: GenericLinux
      versions:
        - all

  galaxy_tags:
    - ibm
    - mas
    - devops
    - rhocp

dependencies:
  - role: ibm.mas_devops.ansible_version_check
