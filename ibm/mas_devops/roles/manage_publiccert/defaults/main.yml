---

mas_instance_id: "{{ lookup('env', 'MAS_INSTANCE_ID') }}"

mas_manual_cert_mgmt: "{{ lookup('env', 'MAS_MANUAL_CERT_MGMT') | default(False, true) }}"
mas_public_cert_data_path: "{{ lookup('env', 'MAS_PUBLIC_CERT_DATA_PATH') | default(\"\", true) }}"
