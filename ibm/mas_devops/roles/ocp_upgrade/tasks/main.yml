---
# 1. Upgrade OCP version
# -----------------------------------------------------------------------------

# https://cloud.ibm.com/docs/openshift?topic=openshift-update#master

# 1.1 Upgrade OCP
- name: "Upgrade cluster {{ cluster_name }} {{ ocp_version_upgrade }}"
  include_tasks: tasks/ocp_upgrade.yml
