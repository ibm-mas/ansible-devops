{
{% if trusted_ca_dict | length > 0 %}
{% for trusted_ca in trusted_ca_dict.keys() %}
{% if loop.index != (trusted_ca_dict | length) %}
  "{{ trusted_ca }}": "{{ trusted_ca_dict[trusted_ca] }}",
{% else %}
  "{{ trusted_ca }}": "{{ trusted_ca_dict[trusted_ca] }}"
{% endif %}
{% endfor %}
{% endif %}
}