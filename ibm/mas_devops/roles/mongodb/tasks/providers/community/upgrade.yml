---

# 1. Running the install action will upgrade MongoDB
# -----------------------------------------------------------------------------
- name: "community : upgrade : Call install task"
  include_tasks: "{{ role_path }}/tasks/providers/community/install.yml"
