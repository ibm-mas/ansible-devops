---
# Task to restore instance when br_skip_instance is set to false
# -----------------------------------------------------------------------------
- include_tasks: "tasks/providers/{{ mongodb_provider }}/restore_instance.yml"
  when: not br_skip_instance | bool

# Task to restore database
# -----------------------------------------------------------------------------
- include_tasks: "tasks/providers/{{ mongodb_provider }}/restore_database.yml"
