apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: "{{ eventstreams_credentials_secret_name }}"
data:
  api_key: "{{ tf_credential_result.outputs.credentials.value.api_key | b64encode }}"
  credentials_all: "{{ tf_credential_result.outputs.credentials.value | b64encode }}"
