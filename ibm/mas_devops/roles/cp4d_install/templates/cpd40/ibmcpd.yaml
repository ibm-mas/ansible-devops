apiVersion: cpd.ibm.com/v1
kind: Ibmcpd
metadata:
  name: ibmcpd-cr
  namespace: "{{ cpd_services_namespace }}"
spec:
  license:
    accept: true
    license: Standard
  storageClass: "{{ cpd_storage_class }}"
  zenCoreMetadbStorageClass: "{{ cpd_metadb_storage_class }}"
