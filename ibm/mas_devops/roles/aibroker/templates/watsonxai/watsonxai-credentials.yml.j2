---
kind: Secret
apiVersion: v1
metadata:
  name: "{{ mas_aibroker_watsonx_secret }}"
  namespace: mas-{{ mas_instance_id }}-aibroker
data:
  api_key: "{{ mas_aibroker_storage_accesskey | b64encode }}"
type: Opaque