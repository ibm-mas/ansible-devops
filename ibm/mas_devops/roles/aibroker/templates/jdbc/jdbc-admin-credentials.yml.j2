---
kind: Secret
apiVersion: v1
metadata:
  name: {{ mas_aibroker_dro_jdbc_secret }}
  namespace: mas-{{ mas_instance_id }}-aibroker
data:
  user: "{{ jdbccfg.username | b64encode }}"
  password: "{{ jdbccfg.password | b64encode }}"
type: Opaque
