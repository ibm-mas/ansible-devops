---
kind: Secret
apiVersion: v1
metadata:
  name: "{{ mas_aibroker_mariadb_secret }}"
  namespace: mas-{{ mas_instance_id }}-aibroker
data:
  username: "{{ mas_aibroker_db_user }}"
  password: "{{ mas_aibroker_db_secret_value }}"
type: Opaque