---
apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: mas-{{ mas_instance_id }}-aibroker
  namespace: "{{ aibroker_namespace }}"
spec:
  targetNamespaces:
    - "{{ aibroker_namespace }}"
