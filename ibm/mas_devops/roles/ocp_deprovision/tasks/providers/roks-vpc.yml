---
# ibm/mas_devops/roles/ocp_deprovision/tasks/providers/roks-vpc.yml
# ROKS VPC clusters use the same deprovision flow as Classic ROKS.
- name: "roks-vpc : Execute roks deprovision tasks"
  ansible.builtin.include_tasks: "{{ role_path }}/tasks/providers/roks.yml"
