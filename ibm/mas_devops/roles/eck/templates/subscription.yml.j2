apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: "{{ eck_package }}"
  namespace: openshift-operators
spec:
  channel: "{{ eck_default_channel }}"
  installPlanApproval: Automatic
  name: "{{ eck_package }}"
  source: "{{ eck_source }}"
  sourceNamespace: "{{ eck_source_namespace }}"
