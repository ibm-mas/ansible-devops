---
- name: Delete WatsonX.AI secret
  command: oc delete secret "{{ tenant_namespace }}----wx-secret" -n {{ aibroker_namespace }}
  register: delete_result
  ignore_errors: yes
  failed_when: delete_result.rc != 0 and "NotFound" not in delete_result.stderr
