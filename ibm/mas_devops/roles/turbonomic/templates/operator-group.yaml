apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: turbonomic-operator-group
  namespace: "{{ turbonomic_namespace }}"
spec:
  targetNamespaces:
    - "{{ turbonomic_namespace }}"
