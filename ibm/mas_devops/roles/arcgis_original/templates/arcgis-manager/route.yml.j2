---
kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: "{{ arcgis_ingress_controller_service_name }}"
  namespace: "{{ mas_arcgis_namespace }}"
spec:
  host: "{{ arcgis_enterprise_fqdn }}"
  to:
    kind: Service
    name: "{{ arcgis_ingress_controller_service_name }}"
    weight: 100
  port:
    targetPort: https
  tls:
    termination: passthrough
  wildcardPolicy: None
