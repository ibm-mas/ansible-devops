suite_wscfg_name: "{{ mas_instance_id }}-watsonsudio-app-{{ mas_application_id }}"
suite_wscfg_labels:
  mas.ibm.com/applicationId: "{{ mas_application_id }}"
  mas.ibm.com/configScope: application
  mas.ibm.com/instanceId: "{{ mas_instance_id }}"
