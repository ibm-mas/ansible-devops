---
# ------ CPD storage classes ------------------------------------------------------------------------------------------
cpd_primary_storage_class: "{{ lookup('env', 'CPD_PRIMARY_STORAGE_CLASS') }}"
cpd_metadata_storage_class: "{{ lookup('env', 'CPD_METADATA_STORAGE_CLASS') }}"

# ------ CPD namespaces -----------------------------------------------------------------------------------------------
common_services_namespace: 'ibm-common-services' # You shouldn't need to change this, is it even supported to change?
cpd_zen_namespace: "{{ lookup('env', 'CPD_SERVICE_NAMESPACE') | default('ibm-cp4d-zen', true) }}"
