---
# CPD Platform Helm component versions for CPD 5.4.0
# All components are installed via Helm (no OLM).
# Only case_version is needed — it is used to locate the correct chart in the Helm repo.
cpd_platform_release_versions:
  postgresql:
    case_version: "5.40.0"
  ibm_pg_operator:
    case_version: "28.3.1"
  ibm_iam_operator:
    case_version: "4.18.0"
  ibm_swhcc:
    case_version: "5.10.0"
  cpd_platform:
    case_version: "5.10.0"
  zen:
    case_version: "6.10.0"
    cr_version: "6.10.0"
  ibm_common_service_operator:
    case_version: "4.19.0"
  ibm_licensing:
    case_version: "4.2.23"
