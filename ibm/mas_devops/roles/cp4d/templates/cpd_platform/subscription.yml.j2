---
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: cpd-operator
  namespace: "{{ cpd_operators_namespace }}"
spec:
  channel: "{{ cpd_channel }}"
  installPlanApproval: Automatic
  name: cpd-platform-operator
  source: "{{ cpd_catalog_source_name }}"
  sourceNamespace: "{{ cpd_catalog_source_namespace }}"
