---
# Fetch all CP4D CRDs dynamically
# cpd_crds: "{{ lookup('pipe', 'oc get crd -o jsonpath=\"{.items[*].metadata.name}\"') | split(' ') | select('search','\\.cpd\\.ibm\\.com$') | list }}"
# cp4d_ns: "ibm-cpd"
# cp4d_operator_ns: "ibm-cpd-operators"
