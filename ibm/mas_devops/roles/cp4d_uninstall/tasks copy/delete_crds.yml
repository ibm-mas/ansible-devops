---
- name: Delete CRD {{ crd_name }}
  shell: |
    if oc get crd {{ crd_name }} &>/dev/null; then
      echo "Deleting CRD {{ crd_name }}"
      oc delete crd {{ crd_name }} --force --grace-period=0 || true
    fi
  ignore_errors: true
