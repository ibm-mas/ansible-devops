---
# this step is only needed for ROKS classic clusters
- name: Disable ocs webhook to allow installing ODF operator
  shell: oc scale --replicas=0 deployment managed-storage-validation-webhooks -n ibm-odf-validation-webhook
  ignore_errors: yes

- include_tasks: "tasks/prereqs/ocs/main.yml"
