---
# 1. Delete DNS Records from Master Account
# ---------------------------------------------------------------------------------------------------------------------
- name: Delete DNS NS type Record in Master CIS Instance
  include_tasks: tasks/provider/ibm/delete_dns_master.yml

# 2. Delete Domain from customer CIS Account
# ---------------------------------------------------------------------------------------------------------------------
- name: Delete DNS NS type Record in Master CIS Instance
  include_tasks: tasks/provider/ibm/delete_cis_domain.yml

# 2. Delete Customer CIS Instance
# ---------------------------------------------------------------------------------------------------------------------
- name: Delete Customer CIS Instance
  include_tasks: tasks/provider/ibm/delete_cis.yml