# Some defaults
ibmcloud_resourcegroup: "{{ lookup('env','IBMCLOUD_RESOURCEGROUP') }}"
ibmcloud_apikey: "{{ lookup('env', 'IBMCLOUD_APIKEY') }}"

# CIS 
cis_action: "{{ lookup('env','CIS_ACTION') | default('provision','True') }}"
cis_instance_name: "{{ lookup('env','CIS_INSTANCE_NAME') }}"
customer_cis_domain: "{{ lookup('env','CUSTOMER_CIS_DOMAIN') }}"