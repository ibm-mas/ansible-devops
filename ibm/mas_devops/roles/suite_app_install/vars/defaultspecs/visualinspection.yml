---
# Default application spec for Visual Inspection
mas_app_spec:
  settings:
    storage:
      size: "{{ visualinspection_storage_size }}"
      storageClassName: "{{ visualinspection_storage_class }}"
