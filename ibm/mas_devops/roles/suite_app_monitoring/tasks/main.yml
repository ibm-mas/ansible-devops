---

# 1. Perform the selected action
# -----------------------------------------------------------------------------
# Two actions are supported:
# - install
# - uninstall
- name: "Perform {{ mas_app_monitoring_action }} action on OpenTelemetry Operator"
  include_tasks: "{{ mas_app_monitoring_action }}/main.yml"
  when:
    - mas_app_monitoring_action != "none"
