---
mas_instance_id: "{{ lookup('env', 'MAS_INSTANCE_ID') }}"
base_output_dir: "{{ lookup('env', 'BASE_OUTPUT_DIR') }}"
collect_certificates: "{{ lookup('env', 'COLLECT_CERTIFICATES') | default(false, true) }}"
additional_namespaces: "{{ lookup('env', 'ADDITIONAL_NAMESPACES') | default('', true) }}"
