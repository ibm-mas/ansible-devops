---
# Configure COS bucket for Manage attachments
# -----------------------------------------------------------------------------
- name: "Configure COS bucket for Manage attachments"
  include_role:
    name: ibm.mas_devops.cos_bucket

- name: Create object storage service credentials secret in Manage namespace
  community.kubernetes.k8s:
    apply: yes
    definition: "{{ lookup('template', 'templates/logging-bucket-creds.yaml.j2')}}"
