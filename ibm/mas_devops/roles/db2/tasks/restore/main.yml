---
# Restore Db2 Universal operator Instance Kubernetes Resources
# -------------------------------------------------------------------------
- name: "Start Db2 Universal operator Instance restore process."
  include_tasks: "{{ role_path }}/tasks/restore/restore-instance.yml"
  when: not br_skip_instance | bool

- name: "Start Database restore process."
  include_tasks: "{{ role_path }}/tasks/restore/restore-database.yml"
