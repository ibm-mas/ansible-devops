---
# Restore Db2 Universal operator instance
- name: "Start DB2 Instance restore process."
  include_tasks: "{{ role_path }}/tasks/restore/restore-instance.yml"

# Restore Db2 database
- include_tasks: tasks/restore_database/main.yml
