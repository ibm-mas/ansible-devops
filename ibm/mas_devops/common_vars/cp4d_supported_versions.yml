cpd_supported_versions:
  - "5.1.3"

# Extract the cpd minor version as there are specific steps to run depending on the cpd minor version defined
cpd_minor_version: "{{ cpd_product_version | regex_search('(?<=)(.*)(?=..)') }}"
