---
- hosts: localhost
  any_errors_fatal: true
  vars:
    file_system_id: "{{ lookup('env', 'FILE_SYSTEM_ID') }}"
    aws_region: "{{ lookup('env', 'AWS_REGION') | default('us-east-1', true) }}"
  roles:
    - ibm.mas_devops.ocp_setup_aws_efs