---
- hosts: localhost
  any_errors_fatal: true
  vars:
    mas_instance_id: "{{ lookup('env', 'MAS_INSTANCE_ID') }}"
    base_output_dir: "{{ lookup('env', 'BASE_OUTPUT_DIR') }}"

  roles:
  # requires login into ibm cloud to run successfully
    - ibm.mas_devops.must_gather
