---
- hosts: localhost
  any_errors_fatal: true

  vars:
    catalog_tag: "{{ lookup('env', 'MAS_CATALOG_VERSION') | default ('@@MAS_LATEST_CATALOG@@', True) }}"
    mas_channel: "{{ lookup('env', 'MAS_CHANNEL') | default ('9.1.x', True) }}"
    mirror_mode: "{{ lookup('env', 'MIRROR_MODE') | default ('direct', True) }}"

  pre_tasks:
    - name: "Load Catalog Metadata"
      ibm.mas_devops.get_catalog_info:
        mas_catalog_version: "{{ catalog_tag }}"
      register: mas_catalog_metadata

    - name: "Check that the catalog is a know catalog"
      assert:
        that: mas_catalog_metadata.failed == false

    - name: "MAS Core Version"
      debug:
        msg: "MAS Core Version: {{ mas_catalog_metadata.mas_core_version[mas_channel] }}"
    
    - name: "MAS Core Version"
      debug:
        msg: "SLS Version: {{ mas_catalog_metadata.sls_version }}"

    - name: "MAS Core Version"
      debug:
        msg: "TSM Version: {{ mas_catalog_metadata.tsm_version }}"
  roles:
    # 1. IBM Maximo Application Suite Core
    # -------------------------------------------------------------------------
    - role: ibm.mas_devops.mirror_images_isc
      when: mirror_mode != "from-filesystem"
      vars:
        package_name: ibm-mas
        package_version: "{{ lookup('env', 'MAS_CORE_VERSION') | default (mas_catalog_metadata.mas_core_version[mas_channel], True) | replace('_', '.') }}"
        
  #   - role: ibm.mas_devops.mirror_images
  #     vars:
  #       manifest_name: ibm-mas
  #       manifest_version: "{{ lookup('env', 'MAS_CORE_VERSION') | default (mas_catalog_metadata.mas_core_version[mas_channel], True) | replace('_', '.') }}"
