---
- name: Install MongoDB Atlas via MAS DevOps role
  hosts: localhost
  connection: local
  gather_facts: false

  vars:
    mongodb_provider: atlas
    mongodb_action: install

  roles:
    - ibm.mas_devops.mongodb
