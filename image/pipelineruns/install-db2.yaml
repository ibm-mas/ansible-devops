apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  generateName: ibm.mas_devops.install-db2-
  labels:
    tekton.dev/pipeline: ibm.mas_devops.install-db2
spec:
  pipelineRef:
    name: ibm.mas_devops.install-db2

  serviceAccountName: pipeline
  timeout: 0s

  workspaces:
    - name: shared-workspace
      persistentVolumeClaim:
        claimName: config-pvc
    - name: shared-settings
      secret:
        secretName: pipeline-settings
