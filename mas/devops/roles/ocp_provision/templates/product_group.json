{
    "name": "{{cluster_name}}",
    "description": "{{cluster_name}}",
    "platform": "x",
    "product_group_id": "{{fyre_product_id}}",
    "ocp_version": "{{ocp_version}}",
    "haproxy": {
        "timeout": {
            "http-request": "10s",
            "queue": "1m",
            "connect": "10s",
            "client": "10m",
            "server": "10m",
            "http-keep-alive": "10s",
            "check": "10s"
        }
    },
    "fips": "no",    
    "worker":  [
        {
            "count": "{{workers_count}}",
            "cpu": "16",
            "memory": "64",
            "additional_disk":  [
                "500",
                "500"
            ]
        }
    ]
}